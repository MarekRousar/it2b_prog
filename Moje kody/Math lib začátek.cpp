#include <stdio.h>
#include <math.h>

int main() {

	int odmocnina = (int)sqrt(4);
	printf("Odmocnina 4: %d", odmocnina);

	//round();
	//floor(), ceil();

	//sin(), cos(), tan();
	
	float absolutni = fabs(-5);
	printf("Absolutní hodnota -5");

	return 0;
}